services:
  agent-laboratory:
    build: .
    container_name: agent-laboratory
    volumes:
      - .:/app
    command: >
      uv run ai_lab_repo.py
      --api-key $API_KEY
      --llm-backend $LLM_BACKEND
      --compile-latex $COMPILE_LATEX
      --config-path $CONFIG_PATH
    ports:
      - "8000:8000"
